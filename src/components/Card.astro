---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---
<div class={`bg-bc-surface border border-bc-border p-8 premium-card-motion ${className}`}>
  <slot />
</div>
